# Hamravesh Deployment Environment Variables
# Copy these values to Hamravesh dashboard when deploying
# Replace placeholder values with your actual Hamravesh domain and settings

# === CRITICAL: Must be set in Hamravesh dashboard ===

# Django Security Settings (SET THESE FIRST)
DEBUG=False
SECRET_KEY=<GENERATE-NEW-SECURE-KEY-USING-DJANGO>
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# Domain Configuration (REPLACE WITH YOUR DOMAINS)
ALLOWED_HOSTS=your-app.hamravesh.ir,your-custom-domain.com
CORS_ALLOWED_ORIGINS=https://your-frontend.hamravesh.ir,https://your-custom-domain.com
CSRF_TRUSTED_ORIGINS=https://your-frontend.hamravesh.ir,https://your-custom-domain.com
API_BASE_URL=https://your-app.hamravesh.ir

# Database Connection String (REPLACE WITH YOUR DATABASE URL)
# Format for PostgreSQL: postgresql://user:password@host:port/dbname
# If using Hamravesh managed PostgreSQL, it will provide this automatically
DATABASE_URL=postgresql://username:password@postgres.hamravesh.ir:5432/image_tagging_db

# Logging Level
LOG_LEVEL=INFO

# === OPTIONAL: Advanced Configuration ===

# Workers (Adjust based on your plan's CPU cores: CPU*2+1)
# Configured in Dockerfile - modify if needed
# GUNICORN_WORKERS=4

# Connection Pool Settings
# CONN_MAX_AGE=600

# Timeout Settings
# REQUEST_TIMEOUT=60

# === HOW TO SET THESE IN HAMRAVESH ===
# 1. Go to Hamravesh dashboard
# 2. Select your application
# 3. Go to "متغیرهای محیطی" (Environment Variables)
# 4. Add each variable from above
# 5. Click "ذخیره" (Save)
# 6. Redeploy the application

# === GENERATE SECRET KEY ===
# Run this command locally:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# Or use: https://djecrety.ir/

# === DATABASE URL FORMAT ===
# PostgreSQL: postgresql://user:password@hostname:5432/dbname
# MySQL: mysql://user:password@hostname:3306/dbname
# SQLite: sqlite:///path/to/db.sqlite3 (NOT recommended for production)

# === COMMON HAMRAVESH DOMAINS ===
# Format: https://your-app-name.hamravesh.ir
# For custom domain: https://your-custom-domain.com
# Get actual domain from Hamravesh dashboard after deployment
